# CLAUDE.mdテンプレート

`{{ }}` の箇所をプロジェクト固有の内容に差し替える。それ以外のルール部分は原則そのまま使う(弱めない)。ゲームエンジンがPhaser以外(Godot等)の場合は、§1と§2の技術名と「ロジック分離」の表現をそのエンジンに合わせて読み替える(思想は同じ: ロジックはエンジン非依存の純関数、シーンは薄い皮)。

---

# CLAUDE.md — {{プロジェクト名}}

このファイルはClaude Codeへの恒久指示である。すべてのセッションで最初にこのファイルとspec.mdを読むこと。

## 0. このプロジェクトは何か

{{1〜3行でプロジェクトの目的。プロトタイプなら「検証したいことは○○の1点のみ」と明記}}

- 仕様の唯一の正はspec.md。このファイルは書き方のルール、spec.mdは何を作るかの定義
- ディレクター(発注者)は非エンジニア。技術的な相談は選択肢と推奨を平易に示すこと

## 1. 技術スタック(固定・変更禁止)

- {{エンジン/フレームワーク}} + TypeScript(strict) + {{ビルドツール}}
- テスト: {{テストフレームワーク}}
- パッケージ管理: npm
- 追加ライブラリの導入は**事前承認制**。無断でnpm installしない。便利系ライブラリも禁止(素のTSで書く)

## 2. ディレクトリ構成(固定)

```
src/
  data/        ゲームデータ定義のみ。ロジック禁止
  systems/     純粋ロジックのみ。{{エンジン名}}のimport禁止
  scenes/      画面({{エンジン名}}側)
  ui/          再利用UI部品
  types/       型定義
```

### 最重要ルール: systems/ はエンジン非依存

- systems/ 内は「入力(状態+経過時間)→出力(新状態)」の純関数で書く
- 理由: (a) テストで数式を直接検証できる (b) 本番エンジンへの移植時にロジックだけ持ち出せる (c) 表示とロジックの混線によるバグを構造で防ぐ
- 画面側はsystemsを呼んで結果を描画するだけの「薄い皮」にする

### データ駆動の徹底

- 名前、数値、テキスト、確率などゲームの中身は**すべて src/data/ に置く**。コードに直書きしない(マジックナンバー禁止)
- バランス調整は data/balance.ts の数値変更だけで完結する状態を保つ

## 3. コーディング規則

- TypeScript strict。`any` 禁止。`as` キャストは原則禁止(必要なら理由をコメント)
- 1ファイル300行を超えたら分割を提案
- 凝った抽象化・デザインパターン・先回りの汎用化は**禁止**。spec.mdにある機能をそのまま素直に書く
- 既存コードの無関係なリファクタリングをしない。頼まれたタスクの範囲だけ触る
- 乱数はseed注入可能な形にする(テストで固定できるように)

## 4. 時間の扱い

{{時間経過が絡むゲームの場合のみ。不要なら削除}}

- 現在時刻をロジック内で直接取得しない。すべて systems/clock.ts の Clock 経由にする
- Clockは倍速係数を持ち、デバッグUIから切替可能にする(長時間の挙動を数秒で検証するため)
- オフライン経過分は「最終保存時刻と現在時刻の差分」から算出する

## 5. セーブデータ

- localStorageにJSON1キーで保存。キー名 `{{save-key-name}}`
- セーブデータに `version: number` を必ず含める
- スキーマ変更時はversionを上げ、旧版読込時は初期化してよい(プロトタイプのため)。初期化した旨をコンソールに出す

## 6. 各タスクの完了条件(Definition of Done)

タスク完了を報告する前に、必ず以下を自分で実行して通すこと。

1. `npm run check`(型チェック)がエラー0
2. `npm run test` が全パス
3. `npm run dev` を起動し、該当機能をブラウザで操作して動作確認した旨と確認手順を報告
4. spec.mdの該当フェーズの受け入れ条件を1項目ずつ照合し、結果を○×で報告

受け入れ条件を満たせない項目がある場合、勝手に仕様を変えて満たしたことにしない。「未達+理由」を報告する。

## 7. 仕様との向き合い方

- spec.mdに書いていないことを実装しない。書いてあることを省略しない
- 仕様が曖昧・矛盾している場合は、実装で勝手に解決せず**質問する**。質問は「A案/B案、推奨はA、理由は〜」の形式で
- 仕様のIDを参照してコミュニケーションする

## 8. Git運用

- コミットは機能単位で小さく。メッセージは日本語1行
- 指示「push」のみで add→commit→push を実行してよい
- mainブランチ直コミットでよい(プロトタイプのため)。初期ブランチ名はmainにする

## 9. エスカレーション基準(上位モデルに渡す領域)

以下に該当する作業は、着手せずに「上位モデル案件」とだけ報告して止まること。

- E-1: アーキテクチャの変更提案(ディレクトリ構成、状態管理方式、シーン構成の変更)
- E-2: 同じバグへの修正を**2回試みて直らなかった**場合。3回目を試みない。現象・試したこと・仮説を整理して止まる
- E-3: パフォーマンス問題の原因調査
- E-4: セーブデータのマイグレーションが本当に必要になった場合
- E-5: バランス数式の再設計(数値変更はよいが、式の構造変更は上位モデル)
- E-6: spec.mdの矛盾が3箇所以上見つかった場合

## 10. 禁止事項まとめ

- 無断のライブラリ追加 / 仕様外機能の実装 / 勝手なリファクタリング
- systems/ へのエンジン依存の持ち込み / 現在時刻の直接使用
- ロジック・データのコード直書き / anyの使用
- 完了条件チェックを省いた完了報告 / 2回失敗したバグの3回目の修正試行
